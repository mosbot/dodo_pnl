# Карточка «Dodotool Финансы» — состояние на 2026-09-05 (v2, вечер)

Приложение `01a072b74fa174b0aec61a57b072b695`, страница
`01a072ccac0d78a18c9783b375d877b1`, OAuth-клиент `cyuqZ` (Authorization Code
Flow). Название выбрал Андрей («Финансы» вместо «Метрики»).

## Заполнено и сохранено (черновик, не опубликовано)

- Название «Dodotool Финансы»; описание 164 зн. (без завершающей точки — с
  ней кабинет ругался «недопустимые символы»; `/` в `UC/KC/DC/LC` тоже
  не прошёл → «себестоимость, ФОТ»).
- Категории: Отчёты, Анализ метрик, Менеджмент, Учет. Концепция Додо,
  Россия. Разработчик Dodotool · https://pnl.dodotool.ru; поддержка
  https://t.me/dodotool. Юрлицо — то же, что у Кассы (ИП, комиссия 20 %).
- Логотип `slides/src/fin-icon-1024.png` (синий #2563eb, глиф модуля
  «столбики» из шапки продукта), фон `fin-logo-bg.png` (светло-синий).
- Карусель (5, `slides/fin/mp_*.png`, 1817×1160, генератор
  `slides/src/compose4.py` — «плашка» в синем #1d4ed8): обложка →
  карточка месяца → P&L по статьям → год к году (+графики) → Пульс.
  Скрины 3x с прода PiX (`shoot3.py`, август-2026, суммы ×0,87), фрагменты
  `slides/fin/frag-*.png`; P&L-таблица склеена без нулевых строк.
- «О приложении»: лид, 7 возможностей, разделы Карточка / P&L / Год к
  году / Пульс с картинками, «Откуда данные», «Кому подойдёт», 3 FAQ,
  alert про компьютер, строка про платформу. «Инструкция»: 4 шага + alert
  про браузеры + «Добавили пиццерию?». Тексты — `fin-card-copy.md`.
- Тарифы (триал 14 дн, `addNewUnitOnOpening: true` — как у Кассы):
  Финансы 1 990 (`01a072dd…`, alias `FINANSY`), Пульс 990 (`01a072e0…`,
  alias `PUL'S`), Финансы и Пульс 2 490 (`01a072e3…`, alias
  `FINANSY-I-PUL'S`). **Alias кабинет генерирует сам** — транслит имени в
  верхнем регистре (у Кассы `KASSA`); в sa `tariff_capabilities` добавлены
  строки под эти алиасы (KASSA→kassa, FINANSY→finance, PUL'S→pulse,
  FINANSY-I-PUL'S→finance+pulse). Без них после снятия safety-net
  маркетплейсная подписка давала бы пусто.

## Не сделано / блокеры

- **Redirect URL клиента `cyuqZ`** — только `https://localhost:5001`. Нужны
  `https://sa.dodotool.ru/dodois/callback` (+ `https://app.dodotool.ru/dodois/callback`)
  — только через заявку в Pyrus (Андрей).
- **sa под два client_id**: логин через нужный клиент по модулю, токены
  per (sub, client_id), синк подписок по обоим приложениям. Пока не начато.
- Пользовательское соглашение (поле пустое, как у Кассы).
- Наблюдение: при открытии страницы приложения фронт кабинета делает
  `POST /manage/api-client/generate-secret` — секрет `cyuqZ` брать из
  кабинета непосредственно перед прописыванием в sa и проверять токеном.

## v2 (2026-09-05, вечер) — переупаковка «от Dodo IS», не от PlanFact

Андрей: франчайзи с PlanFact мало (потому и родился Lite) — P&L не
киллер-фича. Ядро = метрики каждой пиццерии из Dodo IS с целями и зонами +
история метрик по месяцам (клик по карточке в режиме «Период», модалка
`pmModal`, снята `shoot_pm.py`) + Пульс. PlanFact — «если есть, добавит
себестоимость, ФОТ и прибыль».
- Описание (148): «Все метрики каждой пиццерии из Dodo IS на одном экране:
  выручка, кухня, доставка, рейтинги, история по месяцам, цели и зоны.
  Пульс: вся сеть сегодня».
- Карусель: обложка «Все метрики каждой пиццерии — на одном экране, с
  целями» → карточка → история метрик → Пульс → год к году. P&L-кадр
  (`f-5-pnl.png`, «если у вас PlanFact») в запасе, в описании — картинкой.
- «О приложении»: лид от Dodo IS, 7 возможностей, разделы Карточка /
  История / Пульс / Год к году / «Полный P&L — если у вас PlanFact», 4 FAQ.
  Инструкция: PlanFact — шаг 4 «по желанию».
- **Тарифы снижены** (решение делегировано мне): Финансы **1 490**, Пульс
  990, Финансы и Пульс **1 990** (−20 % от суммы). Ступени 1–5/6–15/16–30/
  31–45/46+ по тем же коэффициентам ×1,00/0,85/0,70/0,58/0,50: Финансы
  1 490/1 270/1 040/860/750; Пульс 990/840/690/570/490; пакет
  1 990/1 690/1 390/1 150/1 000. PDF-сетку обновить перед подачей.
- Грабли кабинета (важно): после «Удалить» в диалоге карусели загрузки в
  том же диалоге игнорируются, а «Сохранить изменения» страницы становится
  no-op — надёжнее править слайдер напрямую: `PUT
  /privateapi/web/v1/manage/products/pages/content` с телом
  `{applicationPageId, content:[{alias:'sliders', type:4, content:{value:[ids]},
  language:'ru', version:N}]}` (id кадра — через `content/media` alias
  `sliders`); 409 SimultaneousPageContentChange при неверной version, но
  порядок при этом применился. Editor.js `render()` ~40 с на 30 блоков —
  вызывать без ожидания save в том же вызове.
